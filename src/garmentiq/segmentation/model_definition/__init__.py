# This file can be completely empty! 
"""Segmentation model architectures and their offline configurations.

Holds BiRefNet and the Segment Anything family. Configurations, processors, and
tokenizers are bundled with the package, so only model weights need downloading and
every model can be loaded without network access.
"""
# Its presence alone allows Python to traverse into the /birefnet and /sam folders.